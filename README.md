# Experiview

A monorepo containing the following:

-   A backend consisting of AWS Lambda functions written in Go
-   A frontend consisting of a Vite React TypeScript project deployed using AWS Amplify
-   All backend infrastructure is deployed to AWS using AWS CDK
